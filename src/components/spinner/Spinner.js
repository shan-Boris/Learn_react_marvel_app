import imgSp from './pizzaSliced.gif'
import spinner from './spinner.scss'

const Spinner = () => {
    return (
        <div className="spinner">
            <img src={imgSp} alt="spinner" />
        </div>
        )
    
}

export default Spinner;